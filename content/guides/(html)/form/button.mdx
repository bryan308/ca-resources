---
title: Button
description: The button element creates a clickable button, which can be used to submit forms or trigger actions.
---

### The `button` element

The `<button></button>` element is used to create a clickable button.

The `<button></button>` element can be used to trigger custom JavaScript functions.

<Tabs items={['submit', 'reset', 'button']}>
<Tab value="submit">

```html
<button type="submit">Submit</button>
```

</Tab>
<Tab value="reset">

```html
<button type="reset">Clear</button>
```

</Tab>
<Tab value="button">

```html
<button type="button">Print</button>
```
</Tab>
</Tabs>
